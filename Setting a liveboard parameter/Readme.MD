Runtime Parameters:

This is similar to hiding a liveboard parameter where it set and hide the chips at the same time.

Example:
```
     runtimeParameters:[{
        name: 'oem', 
        value: 'Toyota'
    }]
```