There were two methods in hiding the liveboard parameter. One is by running the Host Parameter and other by runtime Parameters.

Runtime Parameters:

This is similar to setting a liveboard parameter where it set and hide the chips at the same time.

Example:
```
     runtimeParameters:[{
        name: 'oem', 
        value: 'Toyota'
    }]
```

